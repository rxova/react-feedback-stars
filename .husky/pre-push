# Deliberately NOT the browser suite. A pre-push hook that takes 40s gets
# bypassed with --no-verify, and a bypassed hook is worse than no hook.
# Browser tests are CI's job.
pnpm typecheck && pnpm test:unit
